[build]
builder = "dockerfile"

[deploy]
startCommand = "node /usr/local/lib/node_modules/openclaw/dist/entry.js gateway run --port 8080 --auth password"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[variables]
PORT = "8080"
