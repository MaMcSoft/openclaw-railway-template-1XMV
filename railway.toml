[build]
builder = "dockerfile"

[deploy]
startCommand = "node /usr/local/lib/node_modules/openclaw/dist/entry.js gateway run --bind 0.0.0.0 --port $PORT --auth password"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[variables]
PORT = "8080"
